@model IEnumerable<SurveyApp.Models.SurveyGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SurveyGroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SurveyGroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SurveyGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SurveyGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SurveyGroupId })
        </td>
    </tr>
}

</table>
