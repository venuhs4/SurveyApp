@model IEnumerable<SurveyApp.Models.AnalystSurveyType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnalystTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnalystTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnalystSurveyTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnalystSurveyTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnalystSurveyTypeId })
        </td>
    </tr>
}

</table>
